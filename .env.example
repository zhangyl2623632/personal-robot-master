# DeepSeek API 配置
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx



# OpenAI API 配置
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# Moonshot API 配置
MOONSHOT_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# 嵌入模型配置
# 在线模型配置
EMBEDDING_MODEL=BAAI/bge-large-zh-v1.5
# 离线模型配置（如果使用本地模型，请取消下面这行的注释并设置正确的路径）
# EMBEDDING_MODEL_PATH=d:\workSpace-trae\personal-robot-master\models\offline\bge-large-zh-v1.5

# 重排序模型配置
# 在线模型配置
RERANKER_MODEL=BAAI/bge-reranker-large
# 离线模型配置（如果使用本地模型，请取消下面这行的注释并设置正确的路径）
# RERANKER_MODEL_PATH=d:\workSpace-trae\personal-robot-master\models\offline\bge-reranker-large

# 向量存储配置
VECTOR_STORE_PATH=./vector_store

# 本地文档路径
DOCUMENTS_PATH=./data

# 相似度阈值配置（已临时降低到0.15以适应后备嵌入模型）
# SIMILARITY_THRESHOLD=0.15